const color = {
  label: 'Colors',
  options: {
    // load these from elsewhere...
    red: 'red',
    green: 'green'
  },
  defaultValue: 'red',
  groupId: 'Button Options'
};

const icon = {
  label: 'Icon',
  options: {
    // load these from elsewhere...
    plus: 'plus',
    edit: 'edit'
  },
  defaultValue: 'edit',
  groupId: 'Button Options'
};

const size = {
  label: 'Size',
  options: {
    // load these from elsewhere...
    default: 'default',
    large: 'large',
    small: 'small',
    minimal: 'minimal'
  }
};

export { color, icon, size };
